require 'rails_helper'

RSpec.describe GoalsController, type: :controller do
  # @bobbi = User.create(username: "Bobbi", password: "123456")
  # @scruffy = User.create(username: "Scruffy", password: "234567")
  #
  # @goal_private_1 = Goal.new(body: "Get jacked", user_id: @bobbi.id, private: true, completed: false)
  # @goal_private_2 = Goal.new(body: "Stay lit", user_id: @scruffy.id, private: true, completed: true)
  # @goal_public_1 = Goal.new(body: "Go HAM", user_id: @bobbi.id, private: false, completed: true)
  # @goal_public_2 = Goal.new(body: "Level up **prayer-hands**", user_id: @scruffy.id, private: false, completed: false)
  # 
  #
  # describe 'GET #index' do
  #   it "should list all public goals" do
  #     get :index
  #     expect(page).to have_content(@goal_public_1.body)
  #     expect(page).to have_content(@goal_public_2.body)
  #   end
  #
  #   it "should not list any private goals"
  # end
  #
  # describe 'GET #new' do
  #   it "should render the New Goal template"
  # end
  #
  # describe 'POST #create' do
  #   context "with valid params" do
  #     it "should save to the database"
  #     it "should redirect to user's page"
  #   end
  #
  #   context "with invalid params" do
  #     it "should raise an error"
  #     it "should render the New Goal template"
  #   end
  #
  # end
  #
  # describe 'GET #edit' do
  #   it "should render the Edit Goal template"
  # end
  #
  # describe 'PATCH #update' do
  #   context "when user doesn't own goal" do
  #     it "should raise an error"
  #     it "should redirect to the user's page"
  #   end
  #
  #   context "when user owns goal and has provided valid params" do
  #     it "should save to the database"
  #     it "should redirect to user's page"
  #   end
  #
  #   context "when user owns goal and has provided invalid params" do
  #     it "should raise an error"
  #     it "should render the Edit Goal template"
  #   end
  # end
  #
  # describe 'DELETE #destroy' do
  #   context "when user doesn't own goal" do
  #     it "should raise an error"
  #     it "should redirect to the user's page"
  #   end
  #
  #   context "when user owns goal" do
  #     it "should destroy the goal"
  #     it "should redirect to the user's page"
  #   end
  # end
end
